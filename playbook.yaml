---
- hosts: localhost
  connection: local
  tasks:
    - docker_container:
        name: myhello
        image: ufofon/myhello:latest
        state: started
        ports:
          - "80:8080"    

...
